const hre = require("hardhat");

async function main() {
  //
  // set multiplayer dice game as GAME_ROLE on DiceGameHelpers
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
